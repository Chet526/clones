services:
  geobrief:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    volumes:
      - geobrief-data:/data
    restart: unless-stopped

volumes:
  geobrief-data:
